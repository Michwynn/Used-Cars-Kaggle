# Import Libraries
Packages <- c("tidyr", "ggplot2", "dplyr", "tibble", "quantmod", 
              "TTR", "ggthemes", "corrplot" , "Hmisc", "datasets")

lapply(Packages, library, character.only = TRUE)
